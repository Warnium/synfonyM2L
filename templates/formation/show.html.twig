{% extends 'base.html.twig' %}

{% block title %}Formation du bled{% endblock %}

{% block body %}
    <h1>Formation bledard</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formation.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ formation.titre }}</td>
            </tr>
            <tr>
                <th>Datetime</th>
                <td>{{ formation.datetime ? formation.datetime|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ formation.duree ? formation.duree|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Credit</th>
                <td>{{ formation.credit }}</td>
            </tr>

            <tr>
                <th>Credit</th>
                
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_formation_index') }}">retour à la liste</a>

    <a href="{{ path('app_formation_edit', {'id': formation.id}) }}">edit</a>

    {{ include('formation/_delete_form.html.twig') }}
{% endblock %}
